# Frozen core libraries — TECHNICAL.md Section 1.11. Pin exact versions once
# chosen; don't loosen these to "make progress easier" without flagging it.

pydantic>=2.0,<3.0
pyyaml>=6.0
numpy>=1.26

# Pinned per TECHNICAL.md 1.11: Python 3.11.x, PyTorch 2.x / CUDA 12.1 build.
# Install torch separately following the CUDA-12.1 wheel index, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1,<3.0

pytest>=7.4
wandb>=0.16
