# Where the frontend sends API calls. Leave empty to use the dev proxy (/api -> :8000).
VITE_API_BASE_URL=

# "true" starts the app in Demo mode (in-browser mock backend, loudly labelled).
# Default: true, because backend/main.py does not exist yet (FRONTEND_SPEC.md §14).
VITE_DEMO_MODE=true
