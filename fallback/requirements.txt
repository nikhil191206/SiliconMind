# TEMPORARY DEMO FALLBACK server deps (delete with this folder).
# No torch needed: the fallback never imports modules/encoders or modules/generator.
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0,<3.0
numpy>=1.26
scipy>=1.11
pyyaml>=6.0
httpx>=0.27   # FastAPI TestClient, for fallback/test_fallback.py
pytest>=7.4
