"""TEMPORARY DEMO FALLBACK. Delete this whole folder once backend/main.py exists.

Stands in for the parts of the pipeline that cannot run yet (untrained
encoder/generator, no Yosys, no LLM, no DREAMPlace/OpenROAD) so the frontend
can be demoed end to end against real repo code. See FALLBACK.md at the
repo root for exactly what is real, what is substituted, and how to remove it.
"""

FALLBACK_VERSION = "2026-09-24"
